import { defineConfig } from 'vite';

export default defineConfig({
  // Add this line, replacing 'minecraft-demo' with YOUR repository name
  base: '/MarsWarArena/',
  
  build: {
    sourcemap: true,
  },
});