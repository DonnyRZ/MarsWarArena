{
  "name": "minecraftmap-js",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npx vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist" 
  },
  "devDependencies": {
    "vite": "^6.2.0"
  },
  "dependencies": {
    "skinview3d": "^3.1.0",
    "three": "^0.174.0"
  }
}
